import { createAction, handleActions, combineActions } from 'redux-actions';


export const login = createAction('loft-taxi/auth/LOGIN');
export const logout = createAction('loft-taxi/auth/LOGOUT');

  